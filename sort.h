#ifndef SORT_H
    #define SORT_H
    #include "player.h"

    void swap(card_t *card1, card_t *card2);
    void sort(player_t *player, int sort_by);
    
//    SORT BY: CASE 1: by dmg
//             CASE 2: by hp
//             CASE 3: by mana
    
#endif
